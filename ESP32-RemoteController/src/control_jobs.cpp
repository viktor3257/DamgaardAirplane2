#include "control_jobs.h"

// Remote controller control loop jobs will be added here as features grow.
// For now, keep the registration function so the structure mirrors the
// flight controller project.
void register_control_jobs(JobRegistry& R) {
  (void)R;
}
